# See README.md for full notes.
FROM alpine:3.20

# LABEL adds metadata (key=value) to the image.
LABEL maintainer="phireaknoun@gmail.com"
LABEL org.opencontainers.image.title="Dockerfile Lab" \
      org.opencontainers.image.version="1.0" \
      org.opencontainers.image.description="Teaching every Dockerfile instruction"

CMD ["echo", "Inspect me with: docker inspect lab-label"]
