# See README.md for full notes.
FROM alpine:3.20

# ENTRYPOINT sets the executable that ALWAYS runs.
# CMD then supplies the DEFAULT arguments to it.
# On `docker run <image> <args>`, <args> REPLACE the CMD but keep ENTRYPOINT.

ENTRYPOINT ["echo", "Hello,"]
CMD ["world"]
