# See README.md for full notes.
FROM alpine:3.20

# CMD provides the DEFAULT command when the container runs.
# It can be OVERRIDDEN by args on `docker run`.
# Only the LAST CMD in a Dockerfile takes effect.

# Exec form (preferred): a JSON array, no shell involved.
CMD ["echo", "Hello from the default CMD"]
