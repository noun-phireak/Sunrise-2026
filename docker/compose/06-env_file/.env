# The special `.env` file is read AUTOMATICALLY by Compose and is used to
# interpolate ${VARS} inside compose.yaml itself (NOT injected into containers).
# It is different from `env_file:` (which loads vars INTO a container).
COMPOSE_PROJECT_NAME=envfile-lab
