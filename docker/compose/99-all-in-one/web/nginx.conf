server {
    listen 80;

    # Static homepage served by nginx.
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Reverse-proxy /api/ to the `api` service by its Compose DNS name.
    location /api/ {
        proxy_pass http://api:8080/;
    }
}
